apiVersion: apps/v1
kind: Deployment
metadata:
  name: "demo"
spec:
  selector:
    matchLabels:
      app: "demo"
  template:
    metadata:
      labels:
        app: "demo"
    spec:
      containers:
        - name: "demo"
          image: "gcr.io/demo/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "demo"
spec:
  selector:
    app: "demo"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080